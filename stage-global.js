(()=>{const $=s=>document.querySelector(s),stage=$('#stageMode'),open=$('#openStage'),close=$('#closeStage'),draw=$('#stageDraw'),reel=$('#stageReel'),out=$('#stageResult'),title=$('#stageTitle');if(!stage||!open||!draw)return;let kind='candidate',pending=null,axis=$('#stageAxis');if(!axis){axis=document.createElement('select');axis.id='stageAxis';axis.className='stage-axis';stage.querySelector('.stage-main').insertBefore(axis,reel.parentElement)}function store(){return JSON.parse(localStorage.getItem('debate-wheel-v1')||'{}')}function list(){const s=store();if(kind==='candidate')return s.roletas.candidates.items;if(kind==='axis')return s.roletas.axes.items;const used=(s.draws||[]).filter(d=>d.type==='question'&&d.group===axis.value).map(d=>d.item);return(s.roletas.questions.groups[axis.value]||[]).filter(x=>!used.includes(x))}function refresh(){const s=store();axis.innerHTML=Object.keys(s.roletas.questions.groups).map(x=>`<option>${x}</option>`).join('');axis.style.display=kind==='question'?'block':'none';pending=null;draw.disabled=false;draw.textContent=kind==='question'?'Sortear pergunta':'Sortear';title.textContent=kind==='candidate'?'Candidatos':kind==='axis'?'Eixos temáticos':'Perguntas';out.textContent='Pronto para sortear';renderVerticalWheel('stageReel',list())}function sort(){const items=list();if(!items.length){out.textContent='Nenhuma opção disponível';return}if(kind==='question'&&pending){addDraw('question',`Perguntas / ${axis.value}`,pending,axis.value);out.textContent=pending;pending=null;draw.textContent='Sortear pergunta';return}draw.disabled=true;spinVertical('stageReel',items,item=>{out.textContent=item;if(kind==='candidate')addDraw('candidate','Candidatos',item);if(kind==='axis')addDraw('axis','Eixos temáticos',item);if(kind==='question'){pending=item;draw.textContent='Confirmar pergunta'}draw.disabled=false})}open.onclick=()=>{stage.classList.remove('hidden');document.documentElement.classList.add('telão-open');refresh()};close.onclick=()=>{stage.classList.add('hidden');document.documentElement.classList.remove('telão-open')};document.querySelectorAll('.stage-tab').forEach(b=>b.onclick=()=>{kind=b.dataset.stage;document.querySelectorAll('.stage-tab').forEach(x=>x.classList.toggle('active',x===b));refresh()});axis.onchange=refresh;draw.onclick=sort;})();
