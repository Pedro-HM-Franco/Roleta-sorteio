(()=>{setTimeout(()=>{const b=document.getElementById('saveConfig');if(!b)return;b.onclick=()=>{const k='debate-wheel-v1',s=JSON.parse(localStorage.getItem(k)||'null'),clean=v=>v.split('\n').map(x=>x.trim()).filter(Boolean),names=clean(document.getElementById('axisConfig').value),old=s.roletas.questions.groups,fields=[...document.querySelectorAll('.group-config')],byName={};fields.forEach((t,i)=>byName[decodeURIComponent(t.dataset.group)]=clean(t.value));s.roletas.candidates.items=clean(document.getElementById('candidateConfig').value);s.roletas.axes.items=names;s.roletas.questions.groups={};names.forEach((name,i)=>{const previous=Object.keys(old)[i];s.roletas.questions.groups[name]=byName[previous]||old[previous]||[]});localStorage.setItem(k,JSON.stringify(s));location.reload()}},120)})();
